from django.db import models

# Create your models here.

class Product(models.Model):
    col1    = models.TextField(max_length=30)
    col2    = models.TextField(max_length=30)
    col3    = models.TextField(max_length=30)
    col4    = models.TextField(max_length=30)
    col5    = models.TextField(max_length=30)
    col6    = models.TextField(max_length=30)
    col7    = models.TextField(max_length=30)
